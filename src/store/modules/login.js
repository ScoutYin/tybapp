const state = {
  loginVisible: false,
  isLogin: false
}

const getters = {
  loginVisible: (state) => { return state.loginVisible },
  isLogin: (state) => { return state.isLogin }
}

const actions = {

}

const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}
