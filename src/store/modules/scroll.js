const state = {
  // 首页-商城页面的tab内的当前scroll值
  indexStoreTab: []
}

const getters = {
  indexStoreTab: (state) => { return state.indexStoreTab }
}

const mutations = {

}

const actions = {

}

export default {
  state,
  getters,
  mutations,
  actions
}
